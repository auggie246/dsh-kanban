# AGENTS.md

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `auggie246/dsh-kanban`, driven by the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.
